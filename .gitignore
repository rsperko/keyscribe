.DS_Store
.build/
*.app
agent_notes/
.claude/
.codex/
.pi/
.agents/
AGENTS.override.md
CLAUDE.local.md

# Throwaway M0 de-risk spikes — not production (spikes/README.md)
spikes/

# Downloaded STT weights — runtime-downloaded, never committed (design.md §4.1, §5)
models/

# Benchmark corpus — personal voice recordings + run output stay local. The kit (manifest.json,
# record.sh, README.md, and the voices/ generator + analysis scripts) IS committed so the benchmark
# is reproducible against your own voice.
benchmark/*.wav
benchmark/results.json
# voices/ "scratch that" regression corpus: generated clips + generated manifest + local TTS venv
benchmark/voices/*.wav
benchmark/voices/manifest.json
benchmark/voices/.ttsenv
benchmark/voices/.tmpwav
# commands/ verbal-command verification corpus: recorded/TTS clips stay local; commands.json IS committed
benchmark/commands/*.wav


# direnv: local env (real .envrc may hold the notary API key); commit .envrc.example instead
.envrc


# release DMG build artifact (uploaded to GitHub Releases, never committed)
KeyScribe-*.dmg
